# TiDB Fraud Detection — pinned dependencies
#
# Major-version pins prevent silent breakage when upstream ships an API
# rev. Refresh by running `pip install -U -r requirements.txt --dry-run`
# every quarter and reviewing the diff.

# TiDB / MySQL driver
mysql-connector-python>=8.3,<9

# Local embedding inference (semantic memory + vector search seeding)
sentence-transformers>=2.7,<3

# Environment + utilities
python-dotenv>=1.0,<2
faker>=24,<26

# Demo UI (Streamlit dashboards + agent UI)
streamlit>=1.30,<2
altair>=5,<6
pandas>=2.0,<3

# Anthropic SDK (cognitive-foundation agent loop)
anthropic>=0.30,<1
